---
import Printer from "./../../components/icons/Printer.astro";
import { ChevronRight } from "@lucide/astro";
---

<section
  id="animated-section"
  class="py-0 lg:py-16 px-4 rounded-t-[60px] rounded-b-none border-t border-color box-shadow bg-slate-50"
>
  <div class="max-w-[1290px] mx-auto">
    <div class="flex justify-center mb-10 intro-label">
      <div
        class="flex flex-wrap items-center gap-2 px-4 py-2 rounded-lg text-sm font-medium blog-bg-color"
      >
        <p
          class="text-xs font-normal leading-4 text-color border rounded-md pl-1 pr-1 bg-color-card border-color"
        >
          BLOG
        </p>
        <p>Read Matt's newest blog:</p>
        <a href="#" class="underline text-color-primary"
          >Navigating the Labyrinth of Cybersecurity Standards</a
        >
      </div>
    </div>

    <div class="flex flex-col lg:flex-row items-center justify-center gap-10">
      <div class="flex flex-col max-w-md text-left lg:text-right">
        <p class="text-4xl font-medium mb-4 intro-title">Hi, we're Openlane</p>
        <p class="text-lg mb-6 intro-text">
          We're working to develop a cutting-edge cybersecurity and compliance
          automation solution to help organizations of all sizes and industries
          secure their systems, navigate the increasingly complex web of privacy
          laws and regulations, ensure continuous compliance, manage risks, and
          get ahead of evolving cyber threats.
        </p>
        <div class="flex justify-center lg:justify-end intro-cta">
          <button
            class="px-6 py-2 border-2 rounded-full border-brand-400 flex items-center gap-2 font-medium text-base cursor-pointer group/feedback hover:border-primary transition-all duration-300"
          >
            <span
              class="group-hover/feedback:!text-(--color-primary) transition-all duration-300"
              >Learn more about our product</span
            >
            <ChevronRight
              class="w-4 h-4 group-hover/feedback:!text-(--color-primary) transition-all duration-300"
            />
          </button>
        </div>
      </div>

      <div class="w-full max-w-sm flex justify-center intro-printer">
        <Printer />
      </div>

      <div class="flex flex-col max-w-md text-left lg:text-left">
        <p class="text-4xl font-medium mb-4 compliance-title">
          Compliance Automation
        </p>
        <p class="text-lg mb-6 compliance-text">
          We revolutionize how companies are able to fulfill their legal or
          regulatory requirements by leveraging technology to streamline
          previously manual tasks. Automation assures timely adherence to
          regulations and simplifies complex compliance workflows such as risk
          assessments and corrective actions.
        </p>
        <div
          class="flex justify-center lg:justify-start pb-10 md:pb-0 compliance-cta"
        >
          <button
            class="px-6 py-2 border-2 rounded-full border-brand-400 flex items-center gap-2 font-medium text-base cursor-pointer group/feedback hover:border-primary transition-all duration-300"
          >
            <a
              href="/product/compliance"
              class="group-hover/feedback:!text-(--color-primary) transition-all duration-300"
              >Learn more about Compliance Automation</a
            >
            <ChevronRight
              class="w-4 h-4 group-hover/feedback:!text-(--color-primary) transition-all duration-300"
            />
          </button>
        </div>
      </div>
    </div>
  </div>
</section>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";
  import { MotionPathPlugin } from "gsap/MotionPathPlugin";
  import { CSSPlugin } from "gsap/CSSPlugin";

  gsap.registerPlugin(ScrollTrigger, MotionPathPlugin, CSSPlugin);

  document.addEventListener("astro:page-load", () => {
    if (window.innerWidth < 768) {
      return;
    }

    const tl = gsap.timeline({
      scrollTrigger: {
        trigger: "#animated-section",
        start: "top 80%",
      },
    });

    tl.from("#animated-section .intro-label", {
      opacity: 0,
      y: 20,
      duration: 0.5,
    })
      .from("#animated-section .intro-title", {
        opacity: 0,
        y: 40,
        duration: 0.6,
      })
      .from(
        "#animated-section .intro-text",
        { opacity: 0, y: 20, duration: 0.6 },
        "-=0.4",
      )
      .from(
        "#animated-section .intro-cta",
        { opacity: 0, scale: 0.8, duration: 0.6 },
        "-=0.5",
      )
      .from(
        "#animated-section .intro-printer",
        { opacity: 0, x: 100, duration: 0.8 },
        "-=0.5",
      )
      .from(
        "#animated-section .compliance-title",
        { opacity: 0, y: 40, duration: 0.6 },
        "-=0.6",
      )
      .from(
        "#animated-section .compliance-text",
        { opacity: 0, y: 20, duration: 0.6 },
        "-=0.5",
      )
      .from(
        "#animated-section .compliance-cta",
        { opacity: 0, scale: 0.8, duration: 0.6 },
        "-=0.5",
      );
  });
</script>
