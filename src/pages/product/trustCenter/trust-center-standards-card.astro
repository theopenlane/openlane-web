---
type Info = {
  title: string;
  description: string;
  bullets?: string[];
  imageSrc: string;
};

interface Props {
  data: Info;
}

const { data } = Astro.props as Props;
---

{
  data && (
    <div class="rounded-xl shadow p-6 bg-white fade-in-scroll visible">
      <div class="rounded-xl shadow-[0_12px_20px_-12px_rgba(0,0,0,0.25)] mb-6 flex justify-center">
        <img src={data.imageSrc} alt={data.title} class="h-6 w-auto" />
      </div>

      <div class="flex flex-col gap-3">
        <p class="text-[20px] font-medium tracking-[-0.6px] text-left">
          {data.title}
        </p>
        <p class="font-normal text-[16px] leading-5 text-[#6B7682]! text-left">
          {data.description}
        </p>

        {data.bullets && data.bullets.length > 0 && (
          <ul class="space-y-2 pl-5 list-disc marker:text-[#60E8C9] marker:w-2 marker:h-2">
            {data.bullets.map((item) => (
              <li class="text-[#6B7682]! text-[16px] font-normal leading-[20px] text-left">
                {item}
              </li>
            ))}
          </ul>
        )}
      </div>
    </div>
  )
}
